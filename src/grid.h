#pragma once

class Grid{
    public:
    //how many ROWS and columns its contains
    Grid();
    int grid[20][10]; // un array simple con 20 filas y 10 columnas

    private:
    int numRows;
    int numColumns;
    int cellSize;
    
};